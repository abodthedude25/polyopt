// Loop Distribution (Fission) Candidate
// Single loop with multiple independent statements
// Distribution can enable vectorization or parallelization of parts
// Demonstrates: when loop distribution helps

func distribution_candidate(A[N], B[N], C[N], D[N], E[N]) {
    // Single loop with three independent statement groups
    for i = 0 to N {
        // Statement group 1: depends only on A
        B[i] = A[i] * 2.0;
        
        // Statement group 2: depends only on C
        D[i] = C[i] + 1.0;
        
        // Statement group 3: depends only on D
        E[i] = D[i] * D[i];
    }
}

// After distribution:
// for i = 0 to N { B[i] = A[i] * 2.0; }
// for i = 0 to N { D[i] = C[i] + 1.0; }
// for i = 0 to N { E[i] = D[i] * D[i]; }
// 
// Note: Groups 2 and 3 could be fused, but 1 is independent
