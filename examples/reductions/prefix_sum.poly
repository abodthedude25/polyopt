// Sequential Prefix Sum (Scan)
// B[i] = sum(A[0..i])
// Inherently sequential - each output depends on previous
// Demonstrates: strong loop-carried dependency
// Note: Parallel prefix sum algorithms exist but use different structure

func prefix_sum(A[N], B[N]) {
    B[0] = A[0];
    for i = 1 to N {
        B[i] = B[i-1] + A[i];
    }
}
